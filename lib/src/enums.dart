enum TooltipDirection { up, down, left, right }

enum TooltipOffset { none,center, left, right }

enum ShowCloseButton { inside, outside, none }

enum ClipAreaShape { oval, rectangle }

enum Event { show, hide }
